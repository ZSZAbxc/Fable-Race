# Fable Race — 开发日志与路线图

> 朋友间快速联机的 Web 多人赛车游戏。零安装：发一个链接 + 4 位房间码即可开赛。
>
> 本文档两部分：**一、至今的开发日志**（已完成 Phase 0–5，全部经自动化验证）；**二、后续完整计划**（Phase 6–9 及长期方向）。

---

## 快速开始

```bash
pnpm install
pnpm dev          # 同时启动 客户端(5173) + 服务端(2567)
pnpm check        # 三包 TypeScript 类型检查
pnpm smoke        # 物理/赛道冒烟(46项) + 网络端到端(19项)
pnpm --filter @fable/shared probe   # 赛道几何探针（造图标定工具）
```

浏览器打开 `http://localhost:5173`。同一局域网的朋友访问 `http://你的IP:5173`（vite 已开 `host: true`）。

**操作**：W/S 油门/刹车（近停时 S=倒车）· A/D 转向 · 空格 手刹漂移（高速甩头滑出，松开恢复抓地）· R 回最近检查点。

---

## 技术栈与核心决策

| 层 | 选型 | 理由 |
|---|---|---|
| 客户端 | Three.js + TypeScript + Vite | 零安装分发；glTF 生态 |
| 物理 | Rapier (WASM) RaycastVehicleController | 四轮射线悬挂，参数化好调 |
| 网络 | Colyseus 0.15（房间制状态同步） | 自带 Schema 增量同步；房间码加入，无需匹配系统 |
| 结构 | pnpm monorepo：shared / server / client | 车辆/赛道/协议/裁判逻辑三端共享 |

**三大架构原则**（贯穿所有阶段）：

1. **手感本地化**——自己的车本地全速模拟零延迟；远程车快照插值（120ms 缓冲）。
2. **裁判服务器化**——计圈/圈速/名次/结算由服务端权威判定（与客户端共用同一份 `RaceProgress` 纯逻辑，保证一致）。
3. **内容数据化**——加一辆车 = 一个 JSON；加一张图 = 一个 JSON（样条控制点自动生成路面/护栏/检查点/箭头/断口）。

```
packages/
├── shared/    # 纯逻辑（无渲染依赖，可在 Node 无头测试）
│   └── src/
│       ├── data/cars/*.json      车辆注册表（falcon/bison/viper）
│       ├── data/tracks/*.json    赛道注册表（sunrise/ridge/canyon）
│       ├── vehicle/VehiclePhysics.ts   载具物理（漂移/翻车复位/滑行阻力/下压力）
│       ├── track/spline.ts + builder.ts 样条采样/路面/护栏/检查点/挖洞/坠落判定
│       ├── race/RaceProgress.ts  计圈计时（客户端 UI + 服务端裁判共用）
│       └── protocol.ts           消息与载荷定义
├── server/    # Colyseus RaceRoom：大厅阶段机 + 权威裁判 + 结算
└── client/    # 渲染/输入/相机/HUD/菜单大厅/远程车插值/会话生命周期
```

---

# 一、开发日志（Phase 0–5，已完成）

## Phase 0+1 · 骨架与驾驶原型

- pnpm monorepo + 三包 TS 工程；Colyseus 服务跑通；Vite + Three.js 场景。
- Rapier 四轮射线悬挂载具：速度敏感转向、RWD/FWD/AWD、下压力、极速衰减；全部手感参数来自 `cars/*.json`（改保存即生效）。
- 自由试车场：棋盘地面、双坡道、可撞飞木箱、追尾相机（只跟偏航不晕车）、速度 HUD。
- **无头物理冒烟测试**在 Node 中验证坐标约定：+Z 前进、steer+1=左转、刹车/倒车方向（8 项）。

踩坑记录：pnpm 11 构建脚本白名单改为 `pnpm-workspace.yaml` 的 `allowBuilds`；`colyseus` 元包缺 `exports` 导致 Node ESM 命名导入失败（改从 `@colyseus/core` 导入）；Rapier 0.14 前进轴 setter 的怪名 `setIndexForwardAxis`。

## 手感修复批次

- **翻车自动复位**：车顶朝下且停稳持续 2 秒 → 原地扶正（保留位置与朝向）。
- **滑行阻力**：松开油门/刹车后轻微轮刹模拟发动机制动+滚阻，自然减速到停（`engine.coastBrake`）。
- **影视化漂移**（重写手刹）：高速按住空格 → 四轮侧向抓地大降 + 转向变甩头力矩 + 角速度硬限幅——车头甩 60° 而轨迹只偏 30°、保速 71%；松开瞬间咬地。低速空格退化为普通抱死。参数在 `drift` 配置块。
- **空中下压力衰减**：腾空时下压力 ×0.3，飞坡滞空更舒展。

## Phase 2 · 程序化赛道管线

**关键决策**：放弃 Blender+模块拼图，改为**闭环 Catmull-Rom 样条**驱动——十几个控制点自动生成路面 trimesh、红白路缘、护栏、检查点、行进箭头、起点拱门。造新图不需要 3D 软件。

- 计圈系统：有序检查点（防抄近路）+ 正向通过判定（防倒冲线）+ 圈速/最佳圈 + 检查点重生。
- 首批两图：日出环道（新手）、山脊冲刺（带起伏坡）。
- 后续按需求拓宽两次（累计约 88%）：sunrise 12→22.5m，ridge 8.5→15.9m；护栏/触发半径/箭头尺寸全部自动跟随。

## Phase 3 · 联机

- 4 位房间码（去易混字符）创建/加入；大厅：昵称、8 色涂装（localStorage 持久化）、准备状态、房主选图/开赛、房主离开自动迁移。
- 位姿 20Hz 上报 → Schema 增量广播；远程车快照插值 + 轻度外推 + 车顶名牌。
- 3-2-1-GO 锁输入（不是独立阶段：阶段枚举只有 `lobby|racing|results`，倒计时是 `racing` 内基于服务端 `startAt` 的时间闸门）；两列交错起跑格；比赛中途加入自动补位。
- **网络端到端冒烟**：进程内起真实服务端 + 两个 colyseus.js 客户端跑完整流程。

## Phase 4 · 服务端权威比赛循环

- 服务端为每位玩家跑一份 `RaceProgress`（消费客户端上报的位姿+速度），权威判定计圈/圈速/完赛。
  - 注意：20Hz 只是客户端的上报约定，服务端当前不校验频率也不限速，收到即处理。朋友私局无影响，但公网开放前需补上报限流，否则高频上报可加速进度推进。
- 实时名次 500ms 刷新（完赛按冲线顺序，未完赛按进度度量：已过检查点数+推进比例）。
- 第一名完赛后 90s 超时 DNF；全员完赛/超时 → 广播结算（排名/总时间/最快圈）→ `results` 阶段。
- 房主"再来一局"→ 全员状态重置回大厅（可换图）；客户端 `GameSession` 可完整销毁重建，支撑无限循环不泄漏。

## Phase 5 · 内容量产（数据驱动的回报期）

**车辆 ×3**（5 秒实测车速差仅 8 km/h，各有特色不碾压）：

| 车 | 定位 | 特征 |
|---|---|---|
| 猎鹰 Falcon | 均衡后驱 | 全能基准 |
| 野牛 Bison | AWD 重量级 1320kg | 加速/抓地最强、极速最低、漂移沉稳 |
| 蝰蛇 Viper | 轻量后驱 950kg | 极速 178 最高、漂移最刁、抓地最弱 |

选车 UI：车库卡片（极速/加速/抓地/漂移四条属性条 + 定位标语），大厅显示各玩家车型，换车实时同步。

**第三图·峡谷天堑**（2492m，比旧图长 261%，难度★★★★）：

- **4 个 180° 发卡弯**（半径 ~20m）：抓地流被高速转向角限制卡住，漂移可保速甩头——漂移优势弯。
- **断桥飞坡**（全图唯一的高风险点）：8% 上坡冲刺到 y6.1 起跳沿 → **26m 断口**（`gaps` 208–234）→ 对岸桥面 y5.7。实测 140 km/h 起跳腾空至 y9.8 稳落；68 km/h 坠落。三辆车极速都够，靠入弯出弯选线而非车辆解锁。
- **坠落即传送回最近检查点**：掉进断口后判定坠落（低于路面 4m），车被送回**最近一次通过的检查点**并清零速度。惩罚 = 丢掉这段冲刺重跑，不用爬坡也不用等扶正。
- 断口上方自动停发检查点（那里没有路面）；断口边缘有黄黑警示板。

**支撑技术**：路面/护栏按弧长窗口挖洞、检查点排除窗口（`builder.ts` 由 `gaps` 运行时派生，无独立 JSON 字段；服务端/客户端共用同一份）、坠落判定 `hasFallenOff()`（相对路面下沉而非绝对 y，避免低处路段误判）、弧长探针工具 `pnpm probe`（造图标定）。

**已删除的设计**：谷底绕路岔路（坠桥后落到谷底绕行 3-5 秒继续开）与天堑捷径。岔路带来立体交叉护栏、双合法路线计圈、走廊避让三层复杂度，收益不抵成本；改为单一断桥 + 传送重生后，`BranchConfig`/`crossMainWindows`/`wallGaps`/走廊派生逻辑全部移除。

## 当前测试体系（每次改动后全绿）

| 套件 | 命令 | 覆盖 |
|---|---|---|
| 物理/赛道冒烟 53 项 | `pnpm --filter @fable/shared smoke` | 坐标约定/刹滑漂翻/三图几何/计圈/飞坡弹道/坠落传送/三车平衡/全程护栏无缺口 |
| 网络端到端 19 项 | `pnpm --filter @fable/server smoke` | 房间码/大厅/权威计圈/名次/结算/回大厅/房主迁移 |
| 类型检查 | `pnpm check` | 三包 tsc |

---

# 二、后续完整计划（Phase 6–9+）

> 优先级从上到下；每阶段保持"可玩可验收"，冒烟测试随功能同步扩展。

## Phase 6 · 打磨（下一步，建议顺序执行）

**6.1 音效** — ✅ 已完成
- 实现：`packages/client/src/audio/AudioSystem.ts`（全局单例 `audio`）。
- 引擎音走采样而非振荡器：同一采样的三档音高 loop 按归一化 RPM 交叉淡化，
  叠加 ±15% `playbackRate` 微调。RPM 用各车 `config.engine.maxSpeedKmh` 归一化，
  所以慢车快车的声音行程一致。
- 已接：马达发动、胎噪/漂移 loop、手刹（边沿触发，>15 km/h 才响）、撞墙三档、
  落地、倒计时/GO、过检查点、完成一圈、完赛、坠落、菜单点击。
- 撞击靠速度突降检测（`drop > 14 && prevSpeed > 22`，冷却 0.25s），复位与坠落
  重生时清零 `prevSpeed`，避免传送被误判成撞击。
- 音量/静音存 localStorage（`fable.volume` / `fable.muted`），游戏内 `M` 键切换。
- **设置浮层**（`packages/client/src/ui/settings.ts`）：右上齿轮按钮或 `Esc` 打开，
  任意界面可用（菜单/大厅/比赛中/结算）。滑条调总音量（走 master gain，影响所有
  声音，含正在播的引擎 loop）+ 静音按钮。游戏中打开会暂停车辆输入
  （`GameSession.setPaused()` 并入既有 `locked` 闸门），关闭时清残留按键避免
  突然一脚油门。`M` 键改动通过 `onAudioChanged` 回调回写面板保持同步。
  `Input.inFormField()` 让焦点在滑条时键盘归控件，方向键不再开车。
- 浏览器手势解锁：`main.ts` 用一次性全局 pointerdown/keydown 监听触发预载。
- 资产 17 个文件 552 KB，全部外部下载（未自制）。16 个 CC0，胎噪 loop 为
  CC-BY 3.0，署名见 `CREDITS.md`。机器无 ffmpeg，胎噪用 Node 脚本从
  96 kHz/24-bit 重采样到 44.1 kHz/16-bit（843 KB → 258 KB）。
- 加载失败静默降级，不阻塞游戏（`failed` 标志避免每帧重试）。
- ⚠️ 待人工验收：戴耳机跑一圈确认速度感与无爆音，自动化测试覆盖不到听感。

**6.2 视觉反馈**
- 漂移尾烟 + 轮胎痕（贴花条带）、落地尘土、高速速度线；起跳/落地相机轻微 FOV/震动。
- 断口处虚线弹道提示（可选开关）。
- 验收：漂移过发卡弯有"帅"的即时反馈。

**6.3 小地图**
- 由样条采样生成的 2D 俯视轮廓（Canvas），实时绘制各玩家色点与朝向；含岔路线。
- 验收：联机中一眼看清所有人位置。

**6.4 最速圈排行榜（持久化）**
- 服务端 SQLite（better-sqlite3）：每图存 名字/车/最速圈 Top 10；完赛广播时写入。
- 大厅选图时显示该图榜单；打破纪录横幅特效。
- 验收：重启服务器纪录仍在。

**6.5 输入设备**
- Gamepad API 手柄支持（摇杆转向/扳机油门，含死区曲线）；移动端虚拟摇杆+按钮（触屏横屏布局）。
- 验收：手柄漂移可控；手机能完整跑一局。

**6.6 观战与断线**
- 完赛后自动跟拍未完赛玩家（循环切换）；断线 10 秒内允许 Colyseus reconnect 恢复。

## Phase 7 · 资产升级（从网上获取现成资产）

- **glTF 车模管线**：GLTFLoader + 资源缓存；替换占位盒子车。资产：Kenney Car Kit（CC0，自带可染色材质槽，正好对接现有涂装系统）、Quaternius Ultimate Vehicles。`carVisual.ts` 是唯一改动点（当初就是为此抽象的）。
- **场景装饰**：Kenney Racing Kit 的树/岩石/看台/横幅，沿样条按弧长窗口声明式摆放（`decorations` 配置数组，仍然数据驱动）；峡谷图加谷壁岩石。
- **天空与光照**：Poly Haven HDRI 环境贴图替换纯色天空；每图 environment 配置扩展。
- 验收：截图观感上一个档次；加载时间 <5s（glTF 压缩 + 按需加载）。

## Phase 8 · 拓展玩法（架构已预留）

- **时间挑战 + 幽灵回放**：录制 20Hz 位姿快照（复用 RemoteCar 插值直接回放）；本图最速圈幽灵随跑；幽灵数据存服务端。
- **漂移积分赛**：`VehiclePhysics` 已暴露滑移角/漂移状态，按角度×时长×速度计分；新比赛模式 = RaceRoom 阶段机加模式字段。
- **道具系统**（服务端权威事件）：氮气/护盾/减速带；道具箱沿样条声明式摆放；命中判定服务端做。
- **锦标赛**：多局连赛积分表（房间不散，结算累计）。
- **更多内容**：第 4-6 张图（夜景霓虹城、雪山冰面低摩擦、8 字立体交叉）、第 4-5 辆车（漂移特化/越野高悬挂）——全部只是 JSON。

## Phase 9 · 部署上线

- **Docker 单容器**：server 托管 Colyseus + 静态 client 产物（express.static），单端口 2567 出全部流量。
- **方案 A（推荐）**：腾讯云/阿里云轻量 2C2G，国内延迟 20-50ms；Caddy 反代 HTTPS/WSS。
- **方案 B（零成本）**：本机开服 + Tailscale/frp 内网穿透。
- `NetClient` 端点改为可配置（环境变量注入）；生产构建去掉 host:true 调试项。
- 验收：外网朋友输房间码 30 秒内进对局。

## 长期维护原则

- 新内容永远走注册表（cars/tracks/index.ts import 一行）；逻辑改动必须先过 `pnpm smoke`。
- 造图流程：写控制点 → `pnpm probe` 看长度/发卡弯/CP 弧长表 → 标定 gaps/岔路窗口 → smoke 弹道验证 → 试驾微调。
- 手感调参速查：`cars/*.json`（engine/steering/drift/suspension）· 飞坡难度：`tracks/*.json` 的 gaps 窗口宽度 · 漂移优势：发卡弯控制点间距（半径）。

## 已知技术债（低优先级）

- 远程车碰撞为纯视觉（无对撞物理）——朋友局体验可接受，若做碰撞需引入延迟补偿。
- 圈速用服务端收包时刻计时，含单程网络延迟（朋友局 <30ms，误差可忽略）。
- 主环样条自交检测靠 probe 人工核查，未自动化。
- PowerShell 下中文控制台输出偶发乱码（仅影响日志观感）。
